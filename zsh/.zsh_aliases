# improve LS
# -a    include directories starting with a dot
# -h    make the sizes human readable
# -l    list in long format
# -G    colors++
alias ls="ls -ahlG"

# simplify upgrading our system
alias upgrade="upgrade_oh_my_zsh && brew update && brew upgrade && brew cleanup"